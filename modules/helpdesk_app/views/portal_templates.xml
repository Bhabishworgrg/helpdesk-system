<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <template id="portal_support_form_template" name="Support Ticket Form">
        <t t-call="website.layout">
            <div class="container">
                <h1>Submit a Ticket</h1>
				<form t-attf-action="/support/submit?csrf_token=#{request.csrf_token()}" method="post">
                    <div class="form-group">
                        <label for="name">Subject</label>
                        <input type="text" name="title" class="form-control" required="True"/>
					</div>
					<div class="form-group">
						<label for="query">Query</label>
						<input name="query" class="form-control" required="True"/>
					</div>
                    <div class="form-group">
                        <label for="description">Description</label>
                        <textarea name="description" class="form-control" required="True"></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary">Submit</button>
                </form>
            </div>
        </t>
    </template>

	<template id="portal_support_ticket_received_template" name="Support Ticket Received">
        <t t-call="website.layout">
            <div class="container">
                <h1>Thank You!</h1>
                <p>Your support ticket has been received successfully.</p>
            </div>
        </t>
    </template>
</odoo>
